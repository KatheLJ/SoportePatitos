@inherits System.Web.Mvc.WebViewPage<DateTime?>
@if (Model.HasValue)
{
    @Html.TextBox("", String.Format("{0:dd/MM/yyyy hh:mm:ss}", Model.Value), new { @class = "form-control datepicker", @type = "datetime " })
}
else
{
    @Html.TextBox("", String.Format("{0:dd/MM/yyyy hh:mm:ss}", DateTime.Now), new { @class = "form-control datepicker", @type = "datetime" })
}
